class ThemeToggle {
  //extends StateNotifier<bool> {
  ThemeToggle() : super() {
    // setState(false);
  }

  bool get isDarkMode => false; // state ?? false;

  void toggleTheme() {
    // setState(!state!);
  }
}
